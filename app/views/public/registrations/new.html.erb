
<div class = "container">
  <h3 class = "col-4">Musicaを始めよう！</h3>
    <div class = "row" style = "margin: 10px;">

      <div class = "col-5">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%#= render "publics/shared/error_messages", resource: resource %>

          <div class="field">
            <%= f.label :ユーザー名 %><br />
            <%= f.text_field :user_name, class: "form-control" %>
          </div>

          <div class="field">
            <%= f.label :アカウントID %><br />
            <%= f.text_field :account_id, class: "form-control" %>
            <% if @user.errors[:account_id].any? %>
              <div class="error">
                <% @user.errors[:account_id].each do |error| %>
                  <%= error %>
                <% end %>
              </div>
            <% end %>
              <p>※＠と半角英数字、アンダースコアのみ使用可能</br>
              6文字以上で入力してください。</p>
            </div>

          <div class="field">
            <%= f.label :メールアドレス %><br />
            <%= f.email_field :email, class: "form-control" %>
          </div>

          <div class="field">
            <%= f.label :password, "パスワード" %><br />
            <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
          </div>

          <div class="field">
            <%= f.label :password_confirmation, "パスワード再入力" %><br />
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
          </div>

          <div class="actions">
            <%= f.submit "アカウントを作成" %>
          </div>
        <% end %>

        </div>
      </div>
        <div><strong>既に登録済みの方</strong></div>
        <div><%= link_to "こちら", user_session_path %>からログインしてください。</div>
</div>
