
<div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  <h2>プロフィール編集</h2>
    <%= form_with model:@user, local:true do |f| %>
      <div class="form-group">
        <%= f.label :ユーザー名 %>
        <%= f.text_field :user_name, class: "form-control name" %>
      </div>
      <div class="form-group">
        <%= f.label :アカウントID %>
        <%= f.text_field :account_id, class: "form-control name" %>
      </div>
      <div class="form-group">
        <%= f.label :プロフィール画像 %>
          <div class="d-flex align-items-center">
            <%= f.file_field :profile_image, class: "form-control-file user_image", accept: 'image/*' %>
            <%= image_tag @user.get_profile_image, size: '100x100', class: "mt-3 rounded-circle" %>
          </div>
          <div class="form-group">
            <%= f.label :自己紹介 %>
            <%= f.text_area :introduction, class: "form-control introduction" %>
          </div>
          <div class="form-group">
            <%= f.submit "保存" %>
          </div>
    <% end %>
      </div>

  <%= link_to "退会する", users_confirm_withdraw_path(@user), class: "btn btn-secondary" %>
</div>
