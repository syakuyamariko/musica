  <h2><i class="fa-sharp fa-solid fa-bell"></i>通知リスト</h2>
  <% if @notifications.present? %>
    <%= link_to "全削除",notification_path(@notifications), method: :delete,class:"btn btn-light" %>
    <% @notifications.each do |notification| %>
        <%= render "#{notification.action_type}", notification: notification %><!--render "#{notification.action_type}"はaction_typeで特定のビューを呼び出している-->
    <% end %>
  <% else %>
    通知はありません
  <% end %>