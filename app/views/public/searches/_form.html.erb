
<%= form_with url: search_path, method: :get, local: true do |f| %>
 <div class="search-wrap03">
  <%= f.text_field :content, placeholder: "search", class: "search-area" %>
  <button type="submit" aria-label="検索"></button>
 </div>
 <%= f.select :model, options_for_select({"User" => "user", "Post" => "post"}) %>
<% end %>


<%#= f.select :method, options_for_select({"部分一致" => "partial"}) %>
  <!--<input type="text" class="search-area" placeholder="キーワード入力">-->
  <!--<button type="button" class="search-btn">検索</button>-->
<%#= f.select :method, options_for_select({"完全一致" => "perfect", "前方一致" => "forward", "後方一致" => "backward", "部分一致" => "partial"}) %>
